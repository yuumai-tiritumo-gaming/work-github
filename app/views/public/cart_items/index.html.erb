<h2>ショッピングカート</h2>
  <%= link_to "カートを空にする", public_cart_items_clear_path, class: "btn btn-danger", method: :delete %>
<table class='table'>
    <thead>
      <tr>
        <th>(画像)</th>
        <th>商品名</th>
        <th>単価(税込)</th>
        <th>数量</th>
        <th>小計</th>
      </tr>
    </thead>
    <%= render "public/cart_items/all_cart_items", cart_items: @cart_items %>
  </table>

  <%= link_to "買い物を続ける", public_root_path, class: "btn btn-primary" %>
  <h5>合計金額</h5>
  <%= "999999999" %>

  <%= link_to "情報入力に進む", new_public_order_path, class: "btn btn-success" %>